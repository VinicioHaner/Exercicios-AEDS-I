#include <stdio.h>
#include <stdlib.h>
#include "aluno.h"

int main()
{
    tadAniversario data1;
    tadNotas nota1;
    tadAluno aluno1;

    nome = "Arthur"
    nomePtr = &nome;
    endereco = "Florestal";
    enderecoPtr = &endereco

    InicializaNiver(&data1, int 01, int 10, int 2009);
    InicializaNotas(&nota1, float 0, float 0, float 0);
    Inicializa(&aluno1, *nomePtr, 4501, &data1, *enderecoPtr, &nota1, 0);


/*Imprime(tadAluno* aluno);
mudaNota(tadNotas* notas);
defCoeficiente(tadNotas* notas);
*/
    return 0;
}
